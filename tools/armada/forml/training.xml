<tool id="forml-training" name="Training" version="0.1.0" profile="22.01">
    <description></description>
    <requirements>
        <container type="docker" shell="/bin/bash">savannah.ornl.gov/armada/forml/training:0.1.0</container>
    </requirements>
    <command><![CDATA[
        tail -f /dev/null
    ]]></command>
    <inputs>
        <param name="nlcd_raster" type="data" format="tiff" optional="false" label="NLCD Raster" />
        <param name="forseam_data" type="data" format="csv" optional="false" label="ForSEAM CSV" />
    </inputs>
    <outputs>
        <data format="binary" name="output_model" label="Trained Model" />
    </outputs>
    <help><![CDATA[
        Training stage of ForML. Intended to be used as a step in a workflow, but it can be run independently for testing.
    ]]></help>
</tool>
