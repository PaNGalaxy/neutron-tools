<tool id="ml-test-load-prototype" name="MLFlow Import" version="0.1.1" profile="22.01">
    <description>Import a model from MLFlow</description>
    <requirements>
        <container type="docker" shell="/bin/bash">savannah.ornl.gov/ndip/tool-sources/generic/ml-test/prototypes:043f73c9b4fd9c9af5219ce2669e9317b8e182e9</container>
    </requirements>
     <command><![CDATA[
        mkdir model &&
        export MLFLOW_LOGGING_LEVEL=ERROR &&
        pixi run --as-is --manifest-path /src python -m ml_poc
                                --url $url
                                --experiment $experiment
                                load
                                --model models:/$model_name
                                --save-model-path model
                > >(tee -a $output) 2> >(tee -a $output >&2)
    ]]></command>
    <inputs>
        <param name="url" type="text" value="http://10.64.193.19:5000" optional="false"  label="MLFlow tracking server url"/>
        <param name="experiment" type="text" value="wine_quality" optional="false"  label="MLFlow experiment name"/>
        <param name="model_name" type="text" value="" label="Model in MLFlow to import" help="Use format: model_name/version"/>
    </inputs>
    <outputs>
        <data format="txt" name="output" label="Console output"></data>
	    <collection name="output_data" type="list" label="Imported Model ($model_name)">
	        <discover_datasets pattern="__designation__" ext="dat" directory="model" />
	    </collection>
    </outputs>
    <help><![CDATA[
        **ML POC**

        This is a Galaxy tool that imports an ML model from MlFlow to Galaxy.

        For more information, see the repository: https://code.ornl.gov/ndip/tool-sources/generic/ml-test
    ]]></help>
</tool>
