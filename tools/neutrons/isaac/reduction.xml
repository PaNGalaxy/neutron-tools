<tool id="neutrons_isaac_reduction" name="Reduction" version="0.3.0" profile="22.01">
    <description>Runs a Mantid reduction script on a NeXus file to produce a reflectivity curve.</description>

    <creator>
        <person name="Mat Doucet"/>
        <organization url="https://www.ornl.gov/" name="ORNL"/>
    </creator>

    <requirements>
        <container type="docker" shell="/bin/bash">ghcr.io/mdoucet/analyzer:sha-866d5bc</container>
    </requirements>

    <inputs>
        <param name="input_file" type="data" format="h5" label="Input NeXus File" optional="true"/>
        <param name="template_file" type="data" label="Input Template File" optional="true"/>
        <param name="data_dir" type="text" value="/SNS/REF_L/IPTS-34347/nexus" label="Data directory"/>
        <param name="output_dir" type="text" value="/SNS/REF_L/IPTS-34347/shared/isaac" label="Output directory"/>
        <param name="config_json" type="data" format="json" label="JSON Configuration" optional="true" help="A v1 workflow state JSON. When provided, all path/env values come from the state and the individual parameters above are ignored." />
    </inputs>

    <configfiles>
        <configfile name="state_env">#raw
"""Emit shell exports for tool-XML bash from a v1 (or v0) workflow-state JSON.

Slim helper inlined in each tool so the container doesn't need ndip_state
installed. Only emits the env vars the bash actually uses; merge logic is
in the underlying CLIs (simple-reduction / plan-data / analyze-sample /
data-assembler ingest / nr-isaac-format convert-ingest).
"""
import json, os, shlex, sys


def _path(s, key, default=""):
    p = s.get("paths") or {}
    return p.get(key) or s.get(key) or default


state_path, env_path = sys.argv[1], sys.argv[2]
if state_path and os.path.isfile(state_path):
    with open(state_path) as f:
        s = json.load(f) or {}
else:
    s = {}

llm = s.get("llm") or {}
red = s.get("reduction") or {}
seq = s.get("sequence_total")
event_file = _path(s, "event_file") or _path(s, "input_file")

exports = {
    "OUTPUT_DIR": _path(s, "output_directory"),
    "EVENT_FILE": event_file,
    "TEMPLATE": _path(s, "template_file"),
    "DATA_DIR": _path(s, "data_directory") or (os.path.dirname(event_file) if event_file else ""),
    "CONTEXT_FILE": _path(s, "context_file"),
    "REFLECTIVITY_FILE": red.get("result_file") or s.get("result_file", ""),
    "SEQUENCE_TOTAL": "" if seq is None else str(seq),
    "LLM_PROVIDER": llm.get("provider") or s.get("llm_provider", ""),
    "LLM_MODEL": llm.get("model") or s.get("llm_model", ""),
    "LLM_BASE_URL": llm.get("base_url") or s.get("llm_base_url", ""),
}
with open(env_path, "w") as f:
    for k, v in exports.items():
        f.write("export %s=%s\n" % (k, shlex.quote(str(v))))
#end raw</configfile>
    </configfiles>

    <outputs>
        <data format="txt" name="output" label="Reduction Console Output"></data>
        <data format="txt" name="reflectivity" label="Reflectivity File"></data>
        <data format="txt" name="identifier" label="Dataset Identifier"></data>
        <data format="json" name="updated_config" label="Updated JSON Configuration"></data>
    </outputs>

    <command detect_errors="exit_code"><![CDATA[
        #if $config_json
            pixi run --as-is --manifest-path /app python '$state_env' '$config_json' _env.sh &&
            . _env.sh &&
        #else
            export EVENT_FILE='$input_file' &&
            export TEMPLATE='$template_file' &&
            export DATA_DIR='$data_dir' &&
            export OUTPUT_DIR='$output_dir' &&
        #end if
        mkdir -p "\$OUTPUT_DIR" &&
        export HOME=/root &&
        ln -s \$DATA_DIR /app/data &&
        pixi run --as-is --manifest-path /app simple-reduction
                --event-file \$EVENT_FILE
                --template \$TEMPLATE
                --output-dir "\$OUTPUT_DIR"
                #if $config_json
                    --state-in '$config_json'
                #end if
                --state-out '$updated_config'
                > >(tee -a $output) 2> >(tee -a $output >&2) &&
        cat "\$OUTPUT_DIR/.last_reduced_set" > $identifier &&
        COMBINED_FILE=`pixi run --as-is --manifest-path /app python -c "import json,sys; d=json.load(open(sys.argv[1])); print((d.get('reduction') or {}).get('combined_file') or '')" '$updated_config'` &&
        if [ -z "\$COMBINED_FILE" ] || [ ! -f "\$COMBINED_FILE" ]; then
            echo "reducer did not write a usable combined_file to state" | tee -a $output >&2;
            exit 1;
        fi &&
        cat "\$COMBINED_FILE" > $reflectivity
    ]]></command>
</tool>
